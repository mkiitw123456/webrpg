# Public multiplayer endpoint only. Never put secrets in VITE_* variables.
# Vercel environment variables can override this production default.
VITE_GAME_SERVER=wss://webrpg-server.onrender.com/multiplayer
